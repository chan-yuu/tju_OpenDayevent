# AI Vision Lab - 智能视觉识别教学系统

一个完整的AI视觉识别教学平台，集成数据标注、模型训练、物体识别和知识答题四大功能，专为中小学生AI教育设计。

> 🚀 **[快速启动 →](QUICK_START.md)** | 📚 **[学习手册 →](AI_LEARNING_GUIDE.md)** | 🎯 **[答题系统 →](QUIZ_SYSTEM.md)** | 📖 **[详细指南 →](USAGE_GUIDE.md)**

---

## ✨ 核心功能

### 1. 📊 数据标注
- 集成 LabelImg 标注工具
- YOLO 格式标注
- 一键启动标注界面
- 自动管理图片和标注文件

### 2. 🧠 模型训练  
- 基于 YOLOv8 深度学习
- 可视化训练进度
- 自动保存训练模型
- 支持自定义训练参数

### 3. 🔍 智能识别
- 图片上传识别
- 实时相机识别
- 多模型切换
- 置信度显示
- 炫酷扫描动画

### 4. 🎓 知识答题 **[NEW]**
- 20道AI知识选择题
- 三种难度级别
- 实时排行榜
- 即时答案反馈
- 详细知识解析

---

## 🎨 界面特色

- 🌈 **渐变科技感设计** - 紫色、蓝色、粉色动态渐变
- ✨ **粒子动画背景** - 动态光晕效果
- 💫 **扫描线动画** - 识别时的酷炫效果
- 🎯 **侧边栏导航** - 直观的功能切换
- 📊 **实时状态显示** - AI引擎运行状态

---

## 🚀 快速启动

### 方式一：一键安装（推荐）
```bash
cd /home/cyun/Documents/tju-vision-lab
./scripts/install.sh
```

安装脚本会自动：
- ✅ 检查并安装 Node.js 和 Python
- ✅ 安装前端和后端依赖
- ✅ 创建必要的目录
- ✅ 创建桌面快捷方式

### 方式二：桌面快捷方式
```bash
双击桌面 "AI Vision Lab" 图标
```

### 方式三：启动脚本
```bash
cd /home/cyun/Documents/tju-vision-lab
./scripts/start.sh
```

### 方式四：手动启动
```bash
# 启动后端
cd backend && python run.py

# 启动前端（新终端）
npm run dev
```

### 停止服务
```bash
./scripts/stop.sh
# 或按 Ctrl+C (如果使用start.sh启动)
```

---

## 📚 学习路径

### 新手入门
1. 📖 阅读 [AI学习手册](docs/AI_LEARNING_GUIDE.md)
2. 🎯 完成 [答题系统](docs/QUIZ_SYSTEM.md) 测试
3. 📊 尝试数据标注
4. 🧠 训练第一个模型
5. 🔍 体验智能识别

### 进阶实践
1. 收集自己的数据集
2. 训练专属识别模型
3. 优化模型参数
4. 挑战排行榜第一名

---

## 🛠 技术栈

- **前端**：React + TypeScript + Vite + TailwindCSS
- **后端**：FastAPI + Python
- **标注工具**：LabelImg
- **模型**：Ultralytics YOLOv8
- **数据存储**：LocalStorage (排行榜) + 文件系统 (模型/数据集)

## 📦 安装依赖

### 1. 后端 Python 环境

```bash
cd backend
pip install -r requirements.txt
```

依赖包括：
- `fastapi` - Web 框架
- `uvicorn` - ASGI 服务器
- `ultralytics` - YOLOv8 库
- `Pillow` - 图像处理
- `python-multipart` - 文件上传支持

### 2. 前端 Node.js 环境

**要求**：Node.js 14.18+ 或 16+

检查版本：
```bash
node --version
```

如果版本过低，推荐使用 nvm 安装：
```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# 安装 Node.js 18 LTS
nvm install 18
nvm use 18
```

安装依赖：
```bash
npm install
```

### 3. 标注工具 LabelImg

```bash
pip install labelimg
```

## 🚀 快速开始

### 方式一：使用简化命令（推荐）

**启动后端：**
```bash
cd backend
python3 run.py
```

**启动前端（新终端）：**
```bash
npm run dev
```

### 方式二：使用完整命令

**启动后端：**
```bash
cd backend
python3 -m uvicorn main:app --reload --port 8000 --ws none
```

后端将运行在 `http://localhost:8000`

**注意**：
- 使用 `python3 run.py` 是最简单的启动方式
- 或使用完整命令：`python3 -m uvicorn main:app --reload --port 8000 --ws none`
- 添加 `--ws none` 参数禁用 WebSocket 支持（避免依赖问题）

### 第二步：启动前端应用

在新终端窗口：

```bash
npm run dev
```

前端将运行在 `http://localhost:5173`（或 Vite 分配的端口）

### 第三步：在浏览器中打开应用

打开 `http://localhost:5173`，你将看到包含三个步骤的界面。

## 📖 使用流程

### 界面 1: 数据标注

1. **准备图片**：将需要标注的图片放入 `dataset/images/` 目录
2. **启动标注**：在标注界面点击"启动 LabelImg 标注工具"按钮
3. **开始标注**：
   - LabelImg 会自动打开并加载图片目录
   - **重要**：确保选择 **YOLO** 格式（不是 PascalVOC）
     - 点击左侧面板的 "PascalVOC" 按钮，切换到 "YOLO" 格式
   - 使用快捷键 `W` 绘制边界框
   - 为每个框选择或输入类别名称
   - 使用 `D` 键切换到下一张图片
   - 标注会自动保存到 `dataset/labels/` 目录（.txt 文件）
   
**注意**：
- 至少需要 5-10 张已标注的图片才能开始训练
- 标签文件必须是 YOLO 格式（.txt），不能是 XML 格式
- 确保每张图片都有对应的标签文件（图片和标签文件名相同）

### 界面 2: 模型训练

1. **检查数据集配置**：
   - 确保 `dataset/data.yaml` 文件存在并配置正确
   - 确保有标注好的图片和对应的标签文件

2. **配置训练参数**：
   - 设置训练轮数（Epochs），建议测试用 10 轮，生产用 50-100 轮
   - 其他参数已预配置（图像大小 640x640，批次大小 16）

3. **开始训练**：
   - 点击"开始训练模型"按钮
   - **实时查看训练进度**：界面会显示当前训练轮数（如"第 3/10 轮"）
   - 训练在后台异步执行，不会阻塞界面
   - 训练完成后，模型会自动保存并加载
   - 所有训练的模型都会被保存（custom_model, custom_model2, custom_model3...）

**训练状态说明**：
- 📦 正在初始化训练... - 准备训练环境
- 📥 正在加载预训练模型... - 加载 YOLOv8n 基础模型
- 🔄 训练中... 第 X/Y 轮 - 正在训练，显示当前进度
- ✅ 训练完成！ - 训练成功完成

### 界面 3: 目标检测推理

**选择模型**：
- 使用界面顶部的下拉菜单选择要使用的模型
- 可选择预训练模型（YOLOv8n）或自定义训练的模型
- 系统会自动检测所有训练好的模型（custom_model, custom_model2, custom_model3...）
- 默认使用最新训练的模型

**方式一：图像上传**
1. 点击"上传图片"按钮选择测试图片
2. 点击"运行检测"按钮
3. 查看检测结果（边界框和标签）

**方式二：相机实时检测**
1. 点击"使用相机"按钮启动摄像头
2. 点击"拍摄并检测"按钮进行实时检测
3. 查看检测结果
4. 点击"停止相机"结束

## 📂 项目结构

```
neuromark-vision-lab/
├── backend/
│   ├── main.py              # FastAPI 后端服务
│   ├── requirements.txt     # Python 依赖
│   └── runs/                # 训练输出目录（自动生成）
├── dataset/                 # 数据集目录
│   ├── images/              # 训练图片
│   ├── labels/              # YOLO 格式标注文件
│   └── data.yaml            # 数据集配置
├── services/
│   └── pythonService.ts     # 后端 API 调用
├── App.tsx                  # 主应用界面（三个界面）
├── types.ts                 # TypeScript 类型定义
├── package.json             # Node.js 依赖
├── start.sh                 # 快速启动脚本
└── README.md                # 本文档
```

## 🔧 常见问题

### 1. 后端显示离线状态？

- 使用 `python3 -m uvicorn main:app --reload` 启动（避免版本冲突）
- 检查端口 8000 是否被占用：`lsof -i :8000`
- 查看浏览器控制台是否有 CORS 错误

### 2. Node.js 版本太低？

Vite 需要 Node.js 14.18+，检查版本：
```bash
node --version
```

如果版本低于 14.18，参考上方"前端 Node.js 环境"部分升级。

### 3. LabelImg 无法启动？

**问题**：`labelimg: command not found`

**原因**：labelimg 安装在 `~/.local/bin/` 但不在系统 PATH 中

**解决方案（任选其一）：**

1. **使用 Python 模块方式运行（推荐）：**
   ```bash
   python3 -m labelImg dataset/images dataset/labels
   ```

2. **在 Web 界面点击"启动 LabelImg"按钮**（已自动使用 Python 模块方式）

3. **添加到 PATH（一次性永久解决）：**
   ```bash
   echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   ```
   之后就可以直接使用 `labelimg` 命令了

### 4. 训练时出现 "YOLO not installed" 错误？

```bash
cd backend
pip install ultralytics==8.1.0
```

### 5. 训练时出现 "Bad Request" 或 "list index out of range" 错误？

**原因**：数据集太小或标签格式错误

**解决方案**：
1. 确保至少有 **5-10 张已标注的图片**
2. 检查 `dataset/labels/` 中的标签文件：
   - 必须是 `.txt` 格式（YOLO 格式），不能是 `.xml` 格式
   - 每张图片都要有对应的标签文件
3. 检查 `dataset/data.yaml` 配置是否正确：
   ```yaml
   path: ../dataset
   train: images
   val: images
   names:
     - person
     - car
     - dog
     - cat
   ```

### 6. 推理时检测不到目标？

- 确保使用了训练好的模型而不是预训练模型
- 检查测试图片是否包含训练过的类别
- 调整检测置信度阈值（在 `main.py` 的 `model()` 调用中添加 `conf=0.25` 参数）

## 📚 参考资料

- [Ultralytics YOLOv8 文档](https://docs.ultralytics.com/)
- [LabelImg GitHub](https://github.com/HumanSignal/labelImg)
- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [YOLO 数据集格式说明](https://docs.ultralytics.com/datasets/)

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
