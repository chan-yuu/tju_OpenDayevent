还是有问题：前端只能选择自定义，没有预设的场景可以选择，请检查是什么情况导致的。2.开始闭环控制点击后没有任何反应，不应该是出来一辆车然后跟踪轨迹吗？？怎么回事修复一下

不要给我md进行说明文件，让我们专注于代码的任务。1.还是有问题：前端只能选择自定义，没有预设的场景可以选择，请检查是什么情况导致的。2.开始闭环控制点击后没有任何反应，不应该是出来一辆车然后跟踪轨迹吗？？怎么回事修复一下

.控制部分，我要的是真正的闭环控制，用不同的方法控制，每种方法有自己的调参，比如pid的参数，可以看到各种超调，等问题的那种，而不是什么动画播放，去除动画控制，每次控制完成都应该有一个控制偏差图(cte,dhead等等)，以及数据的统计

不要给我md进行说明文件，让我们专注于代码的任务。