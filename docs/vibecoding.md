# 界面
优化一下界面，现在的界面上显示的都是一些目录和步骤信息，不太贴合我的目标（我要做一个感知的demo来进行教学），所以需要的是科技感，优化一下界面（面向中小学生），所以需要比较高大上一点。
# 启动
现在的启动需要我手动进行，cyun@cyun:~/Documents/tju-vision-lab/backend$ python run.py cyun@cyun:~/Documents/tju-vision-lab$ npm run dev 请帮他做成一个桌面快捷方式来快速启动（desktop+sh）,并且通过一个sh来自动根据目录生成desktop放到桌面.
# 数据
教会别人怎么通过数据来分析问题
# 教学
由于是面向中小学生的，请给我一个md列出这部分需要学习的知识，并且再做一个界面来出题，选择题的形式，需要先输入用户名然后实时的进行排行
题目不够多，需要更多的题目，每人从里面随机抽取20道进行答题（保证每个人的题目总分一样）
修复一下答题系统，需要在开始答题的页面上添加排行榜，排行榜保存在本地，同时答题时答案现在无法更换，修改一下。另外每道题都是即答即判定的，并且无论对错都要给出题目的解析

不要给我md进行说明文件，让我们专注于代码的任务。1.现在的启动需要我手动进行，请把它做成一个桌面快捷方式来快速启动（desktop+sh）,并且通过一个sh来自动根据目录生成desktop放到桌面.2.优化一下界面，现在的界面上显示的都是一些目录和步骤信息，不太贴合我的目标（我要做一个感知的demo来进行教学），所以需要的是科技感，优化一下界面（面向中小学生），所以需要比较高大上一点。3.地图编辑单独放在一个panel里面,然后可以自由切换,请给我一个测试用的节点和边的地图关系,一定数量的车辆,让我能通过调度算法来测试调度功能,并且能够控制仿真的速度,可以加快或者减慢调度的速度


不要给我md进行说明文件，让我们专注于代码的任务。1.确实可以调度了，但是车辆电量有点太高了吧？跑了多节点才下降1%肯定不现实。同时车辆的图标有点太小了请给大一点2.车辆管理，需要增添或者删除车辆，可以随机指定，也可以自定义，需要选择车辆的位置，电量等，补充一下。3.任务管理，也是可以增加或者删除任务，增加任务时可以随机指定，也可以自定义任务（定义节点，优先度等）

地图，调度，数据指标，
电量太足
没有电的车辆需要进行充电然后再去完成任务，


不同的调度方法的指标应该是不一样的，再添加一些指标数据，比如总的耗电，总的耗时等等，补充一下